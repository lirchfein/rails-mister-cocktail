<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url(<% if @cocktail.photo.file.nil? %><%= cl_image_path "wsx2vs8barv2ooxvp6k9.jpg", height: 900, width: 1200, crop: :fill %><% else %><%= cl_image_path @cocktail.photo, height: 900, width: 1200, crop: :fill %><% end %>);">
  <div class="banner-content">

    <!-- delete button -->
    <h1><%= @cocktail.name %> <%= link_to "🗑️", cocktail_path(@cocktail), method: :delete, data: {confirm: "Are you sure?" } %></h1>
  </div>
</div>
