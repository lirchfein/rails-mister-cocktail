<h1><%= @cocktail.name %></h1>


<% @cocktail.doses.each do |d| %>
  <p><%= d.description %> | <%= d.ingredient.name %>  <%= link_to "Delete dose", cocktail_dose_path(d.cocktail.id, d), method: :delete, class: "btn btn-primary", data: {confirm: "Are you sure?" } %></p>
<% end %>


<%= link_to "Add dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary" %>

<%= link_to "Back home", cocktails_path, class: "btn btn-primary" %>
